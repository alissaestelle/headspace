const Achievement = ({ title, points }) => {
  return (
    // useEffect(() => {

    // })

    <div className="Achieve-Card">
      <h4>{title}</h4>
      <h3>{points}</h3>
    </div>
  )
}

export default Achievement
